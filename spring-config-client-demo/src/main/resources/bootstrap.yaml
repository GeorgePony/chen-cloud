server:
  port: 8030
spring:
  application:
    name: spring-config-client
  cloud:
    config:
      uri: http://localhost:8081
      profile: dev
eureka:
  client:
    service-url:
      default: http://localhost:8761/eureka/

